load("//installers:data.bzl", "CENTEC_ARM64_PLATFORM")
load("//platforms:defs.bzl", "sonic_platform_manifest")

package(default_visibility = ["//visibility:public"])

sonic_platform_manifest(
    name = "platform",
    data = CENTEC_ARM64_PLATFORM,
)
