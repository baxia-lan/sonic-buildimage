load("//installers:data.bzl", "GENERIC_PLATFORM")
load("//platforms:defs.bzl", "sonic_platform_manifest")

package(default_visibility = ["//visibility:public"])

sonic_platform_manifest(
    name = "platform",
    data = GENERIC_PLATFORM,
)
