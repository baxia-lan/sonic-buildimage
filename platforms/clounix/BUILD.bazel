load("//installers:data.bzl", "CLOUNIX_PLATFORM")
load("//platforms:defs.bzl", "sonic_platform_manifest")

package(default_visibility = ["//visibility:public"])

sonic_platform_manifest(
    name = "platform",
    data = CLOUNIX_PLATFORM,
)
