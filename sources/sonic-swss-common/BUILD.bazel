load("//bazel/sonic/private:sources.bzl", "sonic_source_manifest")

package(default_visibility = ["//visibility:public"])

sonic_source_manifest(
    name = "source",
    source_path = "src/sonic-swss-common",
    source_kind = "submodule",
    upstream_repo = "sonic-net/sonic-swss-common",
    notes = [
        "Shared source tree for libswsscommon and sonic-db-cli during the Bazel migration.",
    ],
)
