load("//bazel/sonic/private:sources.bzl", "sonic_source_manifest")

package(default_visibility = ["//visibility:public"])

sonic_source_manifest(
    name = "source",
    source_path = "src/scapy",
    source_kind = "submodule",
    upstream_repo = "secdev/scapy",
    notes = [
        "Fork-backed development stays on origin/codex while the parent repo consumes the wheel manifest.",
    ],
)
