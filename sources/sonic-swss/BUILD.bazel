load("//bazel/sonic/private:sources.bzl", "sonic_source_manifest")

package(default_visibility = ["//visibility:public"])

sonic_source_manifest(
    name = "source",
    source_path = "src/sonic-swss",
    source_kind = "submodule",
    upstream_repo = "sonic-net/sonic-swss",
    notes = [
        "Primary orchagent-chain source package for the SWSS Debian artifacts.",
    ],
)
