# This file exists only as a compatibility marker for tooling that still expects
# a WORKSPACE root. External dependencies are declared in MODULE.bazel and
# MODULE.bazel.lock.

workspace(name = "sonic_buildimage_legacy_marker")
