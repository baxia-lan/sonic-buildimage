"""docker-config-engine-bullseye — LEGACY: replaced by sonic-common-layer in Phase 2."""

package(default_visibility = ["//visibility:public"])

exports_files(glob([
    "*.j2",
    "Dockerfile*",
    "*.sh",
    "*.conf",
    "*.py",
]))
