# Currently empty — submodule BUILD.bazel files are referenced directly
# by the root repo for deb packaging. The local Bazel registry provides
# @sonic_swss_common for native cc_library builds, but deb packaging
# still uses //src/sonic-swss-common:* paths.
